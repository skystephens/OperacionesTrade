services:
  - type: web
    name: trade-dashboard
    runtime: node
    plan: free
    buildCommand: bash build.sh
    startCommand: npm run preview
    envVars:
      - key: NODE_VERSION
        value: 20
